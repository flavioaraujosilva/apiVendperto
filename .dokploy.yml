version: '1'

project:
  name: vendperto-erp
  description: Sistema ERP VendPerto - API Backend

build:
  context: .
  dockerfile: infra/docker/Dockerfile.backend
  
compose:
  file: docker-compose.yml
  
environment:
  - APP_ENV=production
  - DEBUG=False
  - DATABASE_URL=postgresql://vendperto:nxq8n8m0qh7lnrqz@5.161.218.248:5432/vendperto

healthcheck:
  path: /health
  port: 8001

domains:
  - api.ambientedeteste.dev 